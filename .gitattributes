# Normalize line endings: LF in-repo, native in working tree.
# Prevents CRLF churn from Windows editors touching files authored on *nix CI.
* text=auto eol=lf

# Binaries — never touch.
*.png binary
*.ico binary
*.icns binary
*.zip binary
*.exe binary
